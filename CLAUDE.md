# edo-unity

**安政3年(1856)**の江戸・赤坂／溜池を Unity で再現するプロジェクト。

- Unity **6000.5.2f1** / URP **17.5.0**、シーンは1枚 `Assets/Edo/Scenes/Akasaka.unity`
- 手で書いたアセットは `Assets/Edo/` のみ。`edogoyomi` / `Japanese Castle` /
  `Japanese Village Kit` / `NatureManufacture` / `Waldemarst` は**再配布不可・gitignore**(計約6.9GB)。
  手元に無ければ README.md の手順で import する
- エディタ拡張の入口はすべて Unity メニューの **`Edo/`** の下

**このファイルには不変則とルーティングだけを書く。手順は書かない。**
手順はスキル、シーン固有の状態はメモリに置く(後述の棲み分け表)。

---

## 座標系・寸法・年次(疑わない・変えない)

| | 値 |
|---|---|
| **基準年次** | **安政3年(1856)**。2026-08-29 ユーザー裁定で「嘉永期(1850年前後)」から改めた。**年を1つに固定する** — 「江戸末期」のような幅では、どの図を正とするかで毎回迷う。⚠ **安政江戸地震(安政2年10月2日=1855-11-11)の翌年**。⭕ 描くのは**復旧が済んだ姿**で、被災中の姿は描かない。詳細は `references/sources.md` の [基準年次] |
| **Y = 0** | **海抜0m**。Unity の Y がそのまま標高(m)。2026-08-01 に移行済で、それ以前の Y 値は +25 して読む |
| **X, Z の原点** | 江戸見坂 |
| **柱間** | 江戸間 1間 = 6尺 = **1.818m**。部屋は畳数(1間² = 2畳) |
| **edogoyomi** | 素で置かず **ES = 1.818** を掛ける(`EdoAssets.Eg`) |
| **Japanese Village Kit** | 2.0m/間 なので **`vklib.S = 0.909`** で江戸間に落とす |
| **石垣モジュール** | ピッチ 1.80m / 重ね 0.20m。天端は丸い数字で一直線 |
| **蹴上 / 踏面** | 0.30m / 0.45m — **屋敷の中の石段の既定値**(史料の裏づけは無い)。⛔ **参道の坂には適用しない** — 両端(平面長と比高)が地形と実測で固定されるので、**蹴上と踏面は段数からの従属値**にする(2026-08-24 裁定。汐見坂は同じ蹴上に踏面6.0m) |

---

## 絶対規則

1. **手組み資産は正典。** ユーザーが手で組んだ石垣・長屋・門(`Ishigaki` / `Nagaya` / `Omotemon` ほか)は
   **再生成も削除もしない**。撤去は `SetActive(false)`。
2. **指図を先に起こす。** 屋敷・街区の寸法を動かす前に `docs/Sashizu/` に設計図を描き、
   ユーザーのレビューを受ける。飛ばして2度全面差し戻された。図なら数分、建ててからだと数時間。
   **順序は 設計(指図) → レビュー → 実装 → 指図を更新 → 突き合わせ。**
   実装から指図を生成しない — 先に図を描く関門が消える。
3. **面の高さは地形が決める。** 造成の前に地形を測り、**自然の平場(ベンチ)の高さをそのまま面に採る**。
   窪みは埋めず一段低い郭にして階段廊下でつなぐ。棟が載る所で **|設計面 − 自然地形| ≤ 0.5m**。
   指図には**現況図・切盛図・断面・動線図**を必ず入れる(無い指図はユーザーに出さない)。
   → `unity-buke-yashiki` §B-1 / §B-6、`references/sashizu.md` §3a-§3d
4. **指図は現況だけを載せる。** 過去の案・撤回した説を本文に残さない。経緯は `git log` で追う。
   数値は設計値ファイル(`docs/Sashizu/*.json`)にのみ置き、文章にも図にも**写さない**。
   実装を変えたら指図を更新し、`Edo/<屋敷>/指図と実装を突き合わせる` が 0 件になるまで直す。
   **指図を変えたら実装も流し直す。突き合わせが 0 件でないシーンをユーザーに見せない**
   (2026-08-29: 53件が出たままのシーンで外周の不具合を指摘された。指図では直っていた)。
5. **部材どうしを中心で合わせない。** 隣り合う物は **どの面がどの面に接するか** を指図に書き、
   実装は**置いた駒の実メッシュから面を測って寄せる**。中心で合わせると、部材の実寸が変わった
   瞬間に端が合わず隙間が空く(2026-08-29: 門は開口の芯に、長屋は run の頭から丸ごと並べていて、
   門に 1.66m 食い込み・0.96m 空くのが同時に起きた)。
   → **全体設計(区画・面・棟の並び)と詳細設計(部材どうしの接し方)は別の粒度**。
     `unity-buke-yashiki/references/sashizu.md`「取り合いは面で決める」
6. **建蔽率は敷地全体ベースでのみ出す。** 分母を可建地に替えて数字を作らない。
7. **推定には典拠と確度を付ける**(S/A/B/P/U)。一般類型で埋めた物を既成事実にしない。
8. **自分の成果物を基準に norm を作らない。** 史料値は `estate-types.md` から取る。
9. **地形は現地形に従う** — 街路・坂・水系は現地形。敷地内も**自然の平場を活かし**、
   拝領時造成は最小限にとどめる(規則3)。
10. **開花木(Spring 桜)を置かない。** 季節は春ではない。Summer variant を使う。
    ⛔ **自作の低ポリゴンの木も置かない。**(2026-08-30 ユーザー指示「これは2度と使わないで
    ください。見た目がしょぼすぎます」)`Assets/Edo/Terrain/details/BroadleafTree.prefab`
    (`EdoAssets.Own.Broadleaf`・2,384三角)は**使用禁止**。植栽は**在庫のパックから採る**
    (Waldemarst FreeJapaneseGarden / Japanese Castle Foliage / NatureManufacture)。
    在庫に無い樹種がどうしても要るなら、**在庫の木の作り(枝の分岐・葉のカード・LOD・材質)を
    参考にリアルに再現**して起こす。プリミティブを積んだ代用で済ませない。
11. **区画の座標を C# に書かない。** 町割は `docs/Sashizu/parcels.json` が正典で、ビルダーは
   `EdoParcels.Get("<id>")` で引く。ソースへ数値を戻すと敷地割ツールと実装が別々に動く。
   まだ配列を持っているビルダーは `Edo/敷地割/ビルダーと突き合わせる` が差分を見張っている。
12. **パスの literal を新規に書かない。** すべて `Assets/Edo/Scripts/Editor/EdoAssets.cs` に置く
   (`LoadAssetAtPath` は例外を投げず null を返すので、直書きは静かに壊れる)。
13. **造成前の地盤を Unity から採らない。** 正典は `docs/Sashizu/base_dem.json`(地理院DEM由来・
   造成を一切含まない現代の地面)で、各邸の `*_dem.json` は `Tools/Sashizu/build_base_dem.py` が
   そこから切り出す。live terrain は自他の造成が乗る作業面で、**採る時刻で値が変わる**
   (2026-08-23、松平が朝に流した造成を岡部・土井が昼の標本で「造成前」として吸い込んだ)。
   近代造成を戻した面が要るなら、正本からの**復元レイヤ**を各邸が別に持ち、区画内でクリップする。

14. **図には読める分解能がある。細部を粗い図で否定しない。** Unity の古地図オーバーレイ
   (御江戸大絵図・1枚もの木版)は TPS 補正後も**残差 中央値 55m**。⛔ 数m〜十数mの平面判断に
   使ってはならない。細部は **五千分一東京図(明治16-17年・0.3175 m/px)** と
   **大江戸今昔めぐり**(=『復元・江戸情報地図』安政3年)に当たる。
   ⛔ **縮小した概観で「無い」と判定しない — 原寸で見る**(2026-08-29: 1/4 概観で
   「外堀の折れは無い」と確度Aで断じたが、原寸では新シ橋にはっきり描かれていた)。
15. **屋敷を苗字だけで呼ばない。** 江戸には同姓が非常に多く、**シーンには松平が7家**ある
    (`Edo_Yashiki_Matsudaira` / `MatsudairaDewa` / `Hyuga` / `Mino` / `Mondo` / `Yamato` / `YamatoAkichi`)。
    ⚠ **裸の `Edo_Yashiki_Matsudaira` は当プロジェクトの「松平邸」ではなく、佐賀鍋島家の溜池中屋敷**
    (切絵図の「松平備前守」は「松平肥前守」の誤読)。ここを取り違えると別の屋敷の石垣・区画を根拠にする。
    **当プロジェクトで「松平邸」と言えば 松平出羽守定安(出雲松江藩18.6万石)上屋敷 = `matsudaira_dewa`。**
    識別子は**官位まで入れる**(`matsudaira_dewa` / `matsudaira_yamato` / `sanbezakanishi_matsu` …)。
    **2026-08-30 に改称済み**: 指図 `matsudaira_dewa_*`・生成器 `build_matsudaira_dewa_sashizu.py`・
    `EdoMatsudairaDewaBuilder`・掲示板 `--estate matsudaira_dewa`・`CheckScene("matsudaira_dewa")`。
    ⚠ シーンのグループ名 `Edo_Yashiki_Matsudaira`(=鍋島)は**履歴なので変えない**。
16. **一通に一種別。種別を見出しに立て、すべての項目に番号と題を振る。**
    **【裁定】**(選ぶ)・**【質問】**(事実を答える)・**【報告】**(返事不要)・**【共有】**(返事不要)
    を**混ぜない**。冒頭1行で「何件・何を求めるか」を宣言 → 返事の要るものを先に
    (選択肢は必ず **A/B/C**)→ 返事の要らないものは `---` の下へ。裁定は一通に**最大3件**、
    各件は**6点セット**(どこ/背景2文/選択肢A・B/推奨/影響/裁定図)。**1件に複数の裁定を詰めない。**
    ⛔ **番号は裁定・質問だけでなく報告・共有にも振る** — 返事の要らない項目にもコメントは付く
    (2026-08-30 ユーザー指示)。見出しは `【種別+番号】一行で分かる短い題`。**無題の段落を置かない。**
    ⛔ **地の文の末尾に問いを埋めない** — 読み手に「私は何に答えるのか」を探させない。
    ⛔ **内部の符牒を裸で出さない**(`U12` `EDO-0064` `其十四` `SW2` など)。同じ行に必ず
    **①一行の中身 ②行き先のリンク**(裁定図の URL / 指図のパス+図版番号 / `edo_board.py show EDO-◯◯◯◯`)
    **③どこ** を添える。⭐ 判定法は「**その1行だけ見せて相手が中身を言い当てられるか**」
    (2026-08-31 ユーザー指摘「U12 とか言われてもどんな内容だったか分かりません」)。
    ⛔ **指図はローカルのパスで示さない — 必ず Artifact の URL で示す**(2026-08-31 ユーザー指示)。
    台帳は `docs/Sashizu/README.md` の Artifact 列。更新は**その URL を `url=` に渡して上書き**し、
    ⭐ **ユーザーに見せる前に最新の html を再公開してから**リンクを出す(古い版を指すのは示していないのと同じ)。
    ⭐ 狙いは「**1=A、2=B**」「**報告3だけ違う**」の一言で返せる形。満たせないなら書式が悪い。
    (2026-08-30 ユーザー指摘「どの質問や裁定にどう回答して良いか非常に困る」)
    → 正典 **`docs/reporting-protocol.md`**。**普請奉行・全エージェント・掲示板・セッション間に例外なく効く。**


17. **意匠を決める役と、書き起こす役を混ぜない。** 庭・石垣・建具のように**専門の検分役が
   いる領域の意匠**は、その役に**設計させる**。`edo-sashizukata`(指図方)は「意匠上の判断は
   しない・決まったことを数値へ書き起こすだけ」の役で、**設計能力は無い**。
   ⛔ 普請奉行が素人考えで組んだ案を「決まった大方針」として指図方へ渡さない
   (2026-09-01 に庭でこれをやり、ユーザーに「指図担当に庭を設計する能力はあるのか」と
   止められた)。⭕ 正しい順は **専門役が設計 → 指図方が数値へ書き起こす → 検分役が検める**。
   庭=`edo-niwashi` / 石垣=スキル `unity-modular-stonewall` / 部材=`edo-buzai`。
18. **指図は「誰に検められたか」を持つ。関門が赤なら実装しない・ユーザーに見せない。**
   `docs/Sashizu/<屋敷>_sashizu.json` の `reviews` に検分の結果を記録し、
   **`python3 Tools/Sashizu/review_gate.py`** が見張る(セッション開始の挨拶にも出る)。
   誰が要るかは**指図の中身から決まる**(庭があれば庭方が要る)。指図を書き換えると指紋が
   ずれて自動で「検め直し」になる。⛔ 検分役は read-only なので**呼んだ側が結果を書き戻す**
   (`review_gate.py --record <屋敷> <役> <pass|fail>`)。
   ⚠ 2026-09-01、松江松平の庭が**庭方に一度も検められないまま Stage7 まで実装され**、
   ユーザーに差し戻された。ルーティング表に庭方は載っていたが、**通さなくても何も起きなかった**。

   **【移行期間】2026-09-01 ユーザー裁定(案B)。** 関門は新設なので全6邸が赤で立ち上がった
   (赤17件)。⛔ **全員を即時停止させない** — 赤の大半は「関門が新しくて記録が無い」だけで、
   土井は検図14巡・岡部は検図12巡を実際に通している。移行中の扱いは:
   - ⭕ **検分を通すまで、従来どおり作業を続けてよい**(実装・指図の改訂とも)
   - ⛔ **ただし新たにユーザーへ見せる前には必ず通す。** 見せる=裁定を仰ぐ・レンダを出す・
     指図の Artifact を案内する、のすべて。ここが移行期間中も動かない一線。
   - ⭕ 各邸は「改めて1回、いまの指図で検分に出し `--record` で書き戻す」→ [EDO-0099]
   ⛔ **記録が無い=通していない、ではない。**関門より前の検分は記録されていないだけ。
   ⛔ **それでも遡って `pass` を書かない** — 過去の検分はいまの指図を見ていない。

19. **輪に入っていない値は「未検査」であって「合格」ではない。**
   検査を書いたら**同じ巡で報告経路へ繋ぐ**。設計値を入れたら**同じ巡でそれを描く図を出す**。
   測った ⚠ は stdout に閉じ込めず**成果物(html)に載せる**。
   ⛔ 「書いたのに呼んでいない」は**目視では見つからない** —
   **`python3 Tools/Sashizu/wiring_gate.py`** が全邸を機械で見張る(移植不要)。
   ⛔ **安い輪で見えるはずの欠陥を、高い輪に初めて見つけさせない。**
   欠陥ごとに「それが見える最も安い輪」があり(計算 → 図 → 実装 → ユーザーの目)、
   ⭕ 実装の輪へ進むのは**それでしか見えない型を取りに行くとき**だけ(検分役が緑になってから)。
   ⛔ 実装後に出た指摘を一律に「建てないと分からなかった」と数えない — その多くは**検査の欠落**。
   → `docs/verification-loops.md`

   **【由来】2026-09-02。**松江松平で **同じ型を3度** 見逃した(東庭の平面図・板塀の足元の判定・
   それを直すために書いた水の検査が、いずれも書かれたのに `main()` から呼ばれていなかった)。
   ⭐ 検図3巡かけて見つけた5件を、`wiring_gate.py` は**1秒で全部出す**。
   ⚠ 岡部にも現役の実例が1件ある(`walls_table` が一度も走らない)。

---

## 制作パイプライン

```
① 下書き      Scene ビューで EdoSketch(Edo/下書き, %#d)→ UserData/Sketches/*.json。色が意味を持つ
② 考証 + 指図  切絵図・史料から普請奉行がユーザーと大方針を決める → edo-sashizukata が
              docs/Sashizu/<屋敷>_sashizu.html まで書き起こす(世界座標から SVG を直生成)→ レビュー
   └─ 出す前に  edo-kosho(史実)と edo-kenzu(図の成立)を並列で通す
③ 部材        在庫を先に引く(docs/asset-catalog.md)→ 無ければ Tools/Blender/*.py で新造
④ 登録        EdoAssets.cs にパスを追加(寸法パラメタ化パスは関数で)
⑤ 実装        屋敷のプレハブを解く → ビルダーの Stage を順に実行 → プレハブへ書き戻す(edo-toryo が担当)
   └─ 出す前に  edo-fushin-qa で数値QAと検証レンダ
```

---

## 触ると壊れるもの

- **⚠ このリポジトリは複数の Claude Code セッションが同時に動く。** 2026-08-24 に、広い staging で**他セッションが編集中の指図を巻き込んでコミット**する事故が起きた。
  **作業は `python3 Tools/Session/edo_session.py start <屋敷>` で始める。**
  指図だけなら sparse worktree(12MB)を探して無ければ作って回す。
  Unity を触るなら `start <屋敷> --unity`、Blender で部材を作るなら `start <屋敷> --blender`
  でメインに留まり資源を確保する(⛔ **Blender は worktree では回せない** — 在庫キットが
  再配布不可で gitignore なので来ない)。
  ⚠ **打ち忘れても、他セッションが居る状態で指図に触れば門番がその場で回す**(cd は不要・絶対パスで開く)。
  ⛔ **Unity は排他**(実体が1つで地形の編集は Undo の外)。⛔ **worktree からは Unity を開けない**。
  ⛔ **Unity 作業が終わったら即 `edo_session.py release --resources unity`。**
  セッションを続けるかに関わらず、Unity を使わない時間は握らない(心拍は別作業でも更新されるので、
  放置すると他邸が永久に触れない)。**20分 未使用の unity は待っている側が自動で引き取る。**
  埋まっているときは `edo_session.py wait --resources unity` で**待ち行列**に並ぶ
  (空けば先頭に15分の予約。⛔ **返した側は次の人へ SendMessage で連絡する義務がある**)。
  ⛔ **`git add -A` / `git commit -a` は門番が止める**(パスを明示すること)。
  → `docs/session-coordination.md`

- **屋敷は1軒1プレハブ**(シーンを 245.9MB → 3.06MB にした構造)。ビルダーを走らせる前に
  `Edo/屋敷/編集のためにプレハブを解く(選択中)`、終わったら `Edo/屋敷/プレハブへ書き戻す(全部)`。
  **Revert All を押さない。** プレハブインスタンスの中では再親子付けが黙って無視される。
- **地形の編集は Undo の外。** 触る前に heightmap を `.bin` でスナップショット。
  `Scene.isDirty` は当てにならない。`TerrainData.asset` と `.unity` も退避する。
- **⚠ 2026-08-22 に地形を作り直した。造成は消えている。** ジオリファレンスの 8m ズレを補正し、
  水色の囲いの外の造成を自然地形へ戻した。**屋敷を建てる前に必ず造成ステージを流し直す。**
  面の高さは指図のまま使える(動いたのは面の縁だけ)。→ `docs/terrain-georef-fix.md`
- **コンパイルが止まっていることがある。** `Library/ScriptAssemblies/Assembly-CSharp-Editor.dll` の
  mtime がソースより古ければ実行しない。古いアセンブリで走ると「直したのに反映されない」と誤診する。
- **MCP タイムアウト後の再送で多重実行が起きる。** 冪等でないステージ(特に造成)は
  実行済みかを必ず先に確認する。ガードのマーカーは **active** にする
  (`GameObject.Find` は非アクティブを見つけない)。
- **Blender の FBX を入れたらマテリアルを remap する**(`Edo/御殿/…マテリアルをremap`)。
  やらないと全部真っ白になる。

---

## ルーティング

### 知識の置き場所(同じ事実を二重に書かない)

| 置き場所 | 何を置くか | 判定 |
|---|---|---|
| **メモリ** `~/.claude/projects/-Users-toshio-project-edo-unity/memory/` | このシーン固有の状態と決定 | 「別のシーンでも同じか」→ **No** |
| **スキル** `~/.claude/skills/` | 再利用できるやり方・手順 | 同 → **Yes** |
| **エージェント** `.claude/agents/` | 役割と文脈の隔離。手順は書かず `Skill` で正典を読む | 独立文脈で完結し、小さな結論だけ返せるか |
| **CLAUDE.md**(ここ) | 不変則とルーティングのみ | 毎回必ず効いていてほしい1行か |

### 話題 → 読むもの

| 話題 | まず読む |
|---|---|
| 屋敷の中(建物・庭・整地・建蔽率) | スキル `unity-buke-yashiki` |
| 石垣・城壁・護岸・屋敷囲い | スキル `unity-modular-stonewall`(屋敷より先に) |
| 地表・スプラット・植栽・`execute_code`・検証レンダ | スキル `unity-surface-authoring` |
| Blender で部材を起こす | `Tools/Blender/README.md` + `vklib.py`。⛔ **スキル `blender-modeling` は読まない** — BlenderMCP で live を動かす前提の汎用スキルで、当プロジェクトの「CLI スクリプトが正典」と食い違う |
| Unity MCP の作法 | スキル `unity-mcp-skill` |
| 屋敷・町屋・寺社の**区画そのもの**(敷地割) | `docs/Sashizu/parcels.json` が正典。編集は Unity の `Edo/敷地割`(⌘⇧K) |
| 在庫に何があるか | `docs/asset-catalog.md` → `docs/asset-index.tsv` |
| 指図の描き方・組み方 | `docs/Sashizu/README.md` + `unity-buke-yashiki` の `references/sashizu.md` |
| 地形の座標・造成の初期化・戻し方 | `docs/terrain-georef-fix.md`(2026-08-22 に地形を作り直した) |
| **自分(普請奉行)の権限と境界** | **`docs/fushin-bugyo.md`** — 一邸を預かり大方針を決める役。⛔ 専門役の意匠を自分で決めない・検分を飛ばして見せない・自分で合否を出さない |
| **報告・裁定・質問・共有の書き方**(全経路) | **`docs/reporting-protocol.md`** — 一件一葉。種別ラベル・通し番号・6点セット。⛔ 何かをユーザーに問う前に必ず読む |
| **検査を書いたのに繋いでいない/測ったのに図に出していない/どの輪で検めるか** | **`docs/verification-loops.md`** — 輪の閉じと順序。関門は `python3 Tools/Sashizu/wiring_gate.py`(全邸共通・移植不要) |
| セッション間の報告・裁定要請・情報共有 | `docs/session-board.md` — 節目・ブロッカー・裁定要請は `edo_board.py post`。他邸に効く変更は起票+相手セッションへ直接メッセージ。**自己検図・自己考証はユーザー入力なしに3巡まで** |

### ⭐ あなたは **普請奉行**(一邸を預かり大方針を決める役)

**このセッション自身が普請奉行。** 一つの敷地を預かり、ユーザーと大方針を決め、下の役を呼び分け、
検分の結果を書き戻し、ユーザーへ報告する。⛔ **`.claude/agents/` には無い** — ユーザーと
直接やり取りするのがこの役の定義的な性質で、サブエージェントにはそれができないため。
⚠ 2026-09-02 まで「本文脈」と呼んでいた(他9役が江戸の職名なのにここだけ実装用語だった)。
→ 正典 **`docs/fushin-bugyo.md`**(できること4つ・してはならないこと5つ・1サイクル)

### 作業 → 呼ぶエージェント(`.claude/agents/`)

| 作業 | エージェント |
|---|---|
| 大方針(普請奉行がユーザーと決定済み)を**実装できる数値へ書き起こす** | **`edo-sashizukata`**(指図方・書き込み可) |
| 指図の史実・典拠を検める | **`edo-kosho`**(考証方・read-only) |
| 指図が図として成立しているか検める(断面・造成・重なり) | **`edo-kenzu`**(検図方・read-only) |
| 庭が庭として成立しているか(美観・庭師の技術・時代考証) | **`edo-niwashi`**(庭方・read-only) |
| 在庫に使える物があるか引く | **`edo-zaiko`**(在庫方) |
| Blender で部材を新造する | **`edo-buzai`**(部材方) |
| **指図どおりに Unity へ実装する**(プレハブ解く→Stage実行→書き戻す) | **`edo-toryo`**(棟梁) |
| 建てた後の数値QAと検証レンダ | **`edo-fushin-qa`**(普請検査・計測のみ) |
⛔ **`edo-toryo` / `edo-fushin-qa` を呼ぶ前に、呼び出し元は Unity の claim を返す**か、
検証レンダは呼び出し元が撮る前提で依頼すること。握ったまま呼ぶと、エージェントは
Unity の待ち行列に回されて `execute_code` が一度も通らない(2026-08-31、外堀セッションの実例
— 検査役がレンダを撮れず、結局呼び出し元が自分で撮る羽目になった。EDO-0097)。
| 普請場の巡回・ダッシュボード更新・ユーザーへの翻訳・異常検知 | **`edo-sakuji-bugyo`**(作事奉行・差配役) |

⛔ **指図をユーザーに見せる前・実装に入る前に `python3 Tools/Sashizu/review_gate.py`。**
赤(未検分・不合格・検め直し)の指図は実装しない。⭕ 庭のある指図は **`edo-niwashi` に
設計させてから** `edo-sashizukata` が書き起こす — 指図方に意匠は決められない(規則17・18)。

⛔ **裁定・確認を求めるときは「裁定図」を出す。**(2026-08-30 ユーザー指示)
名前と数字の羅列で選ばせない。**図が無いなら先に図を描いてから**求める。図に載せるもの:
① **どこ**(辺と s・世界座標・図版番号 其◯)/ ② **いま何がどうなっているか**(現況の実測値)/
③ **各案を同じ縮尺で並べた図**(案ごとに別の図にしない — 並べないと差が読めない)/
④ **案ごとに何がどれだけ変わるか**(数値。動く部材・作り直す部材・他邸への波及)/
⑤ **推奨と理由**。⛔ 「A か B か」だけ書いて寸法も図も付けないのは差し戻しの対象。

⛔ **報告・連絡・相談は `docs/reporting-protocol.md` の作法で書く(規則16)。**
種別(【裁定】【質問】【報告】【共有】)を見出しに立て、返事の要るものに番号を振り、
返事の要らないものと混ぜない。**普請奉行・エージェント・掲示板・セッション間すべてに効く。**

⛔ **裁定・確認を求めるときは「どこ」を必ず示す。**図版番号(其◯)・辺と s・グリッド(u,v)・
世界座標・隣接物のうち、**相手が図の上で指させるものを最低1つ**添える。名前だけで出さない
(2026-08-29「どこのことを指してるのか分からない」でユーザーから差し戻された)。
図がまだ無いなら**先に図を出してから**裁定を求める。

**指図の"大方針"(配置・史料解釈・意匠)を決めるのは普請奉行**(ユーザーと対話しながら詰める判断のため)。
それを実装できる数値へ**書くのは `edo-sashizukata`**(⚠ 2026-08-29 に「作事方」を名乗ったが、
史実の作事方は工事を管理する行政組織であって図面を引く個人ではなかったため「指図方」に訂正した)。
エージェントは**書く側**(sashizukata)・**検める側**(kosho/kenzu/niwashi)・**実行する側**
(toryo/buzai)・**照会する側**(zaiko)・**検査する側**(fushin-qa)・**差配する側**(sakuji-bugyo)に分かれる。

⚠ **「作事奉行」と「門番」は別物。** 2026-08-29 まで両方が「司令塔」を名乗っていて紛らわしかった。
**作事奉行**は巡回する人(`.claude/agents/edo-sakuji-bugyo.md`・呼ばれたときだけ動く)、
**門番**は claim の衝突を自動で止める機構(`Tools/Session/edo_session.py` + `.claude/hooks/`)。
古い board の記録・過去のコミットには「司令塔」の名で残る(履歴なので書き換えない)。
⚠ **`edo-toryo` は指図に無い値を発明しない** — 足りなければ `edo-sashizukata` の書き起こし漏れ
として差し戻すか、設計判断が要るなら普請奉行(ユーザー裁定)へ回す。実装中に踏んだ非自明な罠は
`unity-buke-yashiki/references/qa-and-pitfalls.md` へ必ず書き戻す(エージェントは毎回記憶ゼロで
起動するため、書き戻しだけが技法の引き継ぎ手段)。
