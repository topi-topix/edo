---
name: unity-surface-authoring
description: >-
  Unity(URP)で「表面」をリアルに作るための実証済み技術集 — 地形のスプラットマップ/地表テクスチャ、
  詳細植生(草・低木)、マテリアル、古地図から地形へ道や水を写すこと。あわせて、それに毎回必要になる
  土台も扱う: mcp__unityMCP__execute_code で C# を走らせる、オフスクリーンレンダで結果を確かめる、
  地図オーバーレイのジオリファレンス、地形の造成とベイク範囲、Sceneビューの操作不具合。
  Unity MCP 越しに「地面・地形・マテリアルがどう見えるか」を触るときは必ず使うこと —
  地形をリアルにする、スプラットを塗る、草を生やす、タイリング/ノーマル/PBRの調整、
  地図から道や水を地形へ写す、変更を確認するスクリーンショットを撮る、など。
  「地形/地表/土/草/テクスチャ/表面をリアルに」のように API 名が出ないときも発火させること。
  石垣そのものは unity-modular-stonewall、屋敷の中身(建物・庭)は unity-buke-yashiki。
---

# Unity Surface Authoring (via MCP)

**この SKILL.md は原則と索引。** 手順とコードは `references/` にある。

このユーザーの Unity プロジェクト(edo-unity / edo-assets、URP、Unity 6000.x)で実際に成果が出た
技術のカタログ。Unity-MCP の一般的な操作(GameObject・シーン・テスト)は `unity-mcp-skill` を、
石垣の配置は `unity-modular-stonewall` を、屋敷の中身は `unity-buke-yashiki` を見る。
ここは**表面を正しく見せること**と、そのために必要な土台に特化している。

---

## 原則

**Golden rule: 見えない表面は作れない。** 視覚的な変更は必ずレンダしたスクリーンショットで確認する。
高価な塗りは、コミットする前にプレビューで検証する。

- **変更 → スクショ → 読む → 反復。** 塗りが効いたと仮定しない。見る。
- 全地形にかかる重い操作は、粗いグリッドかプレビュー画像でルールを試してから本番へ。
  1Mテクセルの逆投影を 65×65 の粗グリッド+バイリニアに置き換えても誤差はサブメートル、速度は桁違い。
- **ベイク・塗り・造成は「範囲限定」を既定にする。** 全域を対象にすると、無関係の場所を壊した事故が
  実際に起きた(`references/terrain-editing.md`)。
- **地形編集は `Undo` の対象外。** 触る前にハイトマップを `.bin` でスナップショットする。
  これが唯一の戻り道。
- **自分が起こす `SaveScene` / `SaveAssets` の前に `.unity` と `TerrainData.asset` を退避する。**
  `Scene.isDirty` は「メモリ==ディスク」の信号として信用できない(ドメインリロード直後は false でも
  中身が違うことがある)。保存するとユーザーの未保存編集も一緒にコミットされる — 無害だが検出して報告する。
- 再利用できるエディタ道具はプロジェクトの `Editor/*.cs` として残す。
  **このシーン固有の事実はメモリへ**、再利用できる技術はこのスキルへ(同じ事実を両方に書かない)。

---

## 参照ファイル

| ファイル | 中身 | いつ読むか |
|---|---|---|
| `references/execute-code-and-render.md` | **execute_code の罠**(codedom の制約・タイムアウト・多重実行)、オフスクリーンレンダの撮り方、使い捨て Terrain での試験、検証レンダの霞=フォグ | 何かする前に一度。C#を書くとき/撮るとき |
| `references/splatmaps.md` | 地形スプラットマップ、リアルな土の地表 | 地面の色・質感を作るとき |
| `references/vegetation.md` | 詳細植生(交差クアッド>ビルボード)、密度・分布・パフォーマンス | 草・下草を生やすとき |
| `references/map-features-and-georef.md` | 古地図から道・水を地形へ写す、土地利用ゾーンでのプレハブ配置、**地図オーバーレイのジオリファレンス** | 地図を根拠に何か置くとき |
| `references/meshes-on-terrain.md` | 石垣のスカートメッシュ、**段坂**(2mハイトマップでは作れない段の道)、アトラスからのUV流用 | 地形に構造物を馴染ませるとき |
| `references/terrain-editing.md` | 近代DEMの穴埋め、回転グリッドの段丘造成、複数区画の一括造成、**ベイクの範囲限定**、Bank Smooth後の水際の隙間 | 地形を造成・修復するとき |
| `references/water-effects.md` | 水中表現・スクリーンスペースの水ボリューム(URP) | 水中に潜る表現 |
| `references/workflow-and-pitfalls.md` | 作業方法、再datum化、Sceneビューのズーム貫通、**Bloom白球=法線ゼロ→NaN**、現地メモ | 詰まったとき |
| `references/recipes.md` | 長いコードスニペット集 | コードをコピーするとき |

## 他スキルとの境界(同じ事実を二重に書かない)

- **石垣・城壁・護岸のモジュール配置** → `unity-modular-stonewall`
  (出隅のR1/R2、天端、ピッチ1.80m、汀線合わせ、壁まわりの造成)
- **屋敷の敷地の中身** → `unity-buke-yashiki`
  (整地の原則、連続御殿複合＋外周長屋帯、(u,v)フレーム、クリアランス、OBB QA、
  アセットパレット、庭園と池、切絵図の考証)
  ⚠ かつてここに「屋敷内部の配置」を丸ごと書いていたが、`unity-buke-yashiki` と二重管理になり
  ドリフトしたため削除した。屋敷の中身の知見はそちらへ書く。
- **このシーン固有の状態**(区画の正体・実装済みの街区・ユーザーの決定) → プロジェクトメモリ

## このスキルの育て方

新しく実証した技術は **`references/` の該当ファイル**へ追記する。長いコードは `recipes.md` へ。
新しい領域(シェーダ・デカール・VFX・LOD・テクスチャベイク等)が出てきたら
`references/<domain>.md` を新設し、この索引に1行足す。

SKILL.md 本体(原則+索引)に手を入れるのは、**作業方法そのものが変わったとき**だけ。500行以内に保つ。

- **追記の前に上の「原則」と矛盾しないか確認する。** 矛盾する古い記述は消すか「撤回」と明記して移す —
  **両論併記のまま残さない**(`unity-buke-yashiki` でこれを放置して手戻りが出た)。
- **一般化して書く。** プロジェクト固有の値(レイヤー名・geoCenter・原点の緯度経度)を書くときは
  そうと明示する。

## ⛔ 自作の低ポリゴンの木を置かない ★★★

**2026-08-30 ユーザー指示** — 「邸内に自作の低ポリゴンの木が配置されています。
**これは2度と使わないでください。見た目がしょぼすぎます。** assets のものを利用するから、
assets を参考にリアルに再現したものを利用してください」

⛔ `Assets/Edo/Terrain/details/BroadleafTree.prefab`(`EdoAssets.Own.Broadleaf`・**2,384三角**)は
使用禁止。在庫の木(Waldemarst FreeJapaneseGarden の黒松・桜・竹、Japanese Castle の Foliage、
NatureManufacture)と並べると、枝の分岐も葉のカードも粗さが一目で分かる。

**植栽は在庫のパックから採るのが既定。** 在庫の引き方は `docs/asset-catalog.md` §9 →
`docs/asset-index.tsv`(ポリ数・実寸つき)。⇒ 置く前に必ず在庫を引く。

**在庫に無い樹種がどうしても要るとき**(常緑広葉樹・梅など。JG パックには黒松・桜・竹しか無い)は、
**在庫の木の作りを参考にリアルに再現**する:

- 幹と主枝は**実ジオメトリ**で分岐させる(円錐を積まない)
- 葉は**カード(クロスした板)にアルファのテクスチャ**。在庫の木の葉テクスチャを流用する
  (材質を新規に作らない — キットの材質名を保つのが規約)
- **LOD を持たせる**。在庫の木は LOD 付きなので、無いと遠景でだけ浮く
- ポリ数は在庫の同格の木に合わせる(JG の黒松 Mid で数千〜1万三角)

⛔ **「遠景だから粗くてよい」で通さない。** ユーザーは邸内の近景で見ている。
