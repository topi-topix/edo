# シーンの重さを構造的に下げる（作業計画・指図）

作成 2026-08-15 / **B1・B2 は 2026-08-16 に実施完了(結果は §7)。B3 は未着手。**
前段: `lfs-history-rewrite.md`（LFS履歴の掃除、実施済）
関連メモリ: `akasaka-scene-bloat` / `edo-github-repo` / `pond-snap-overlaps-in-scene`

---

## 1. 出発点

`Assets/Edo/Scenes/Akasaka.unity` は **102.5 MB**。LFS はバージョンごとに丸ごと保存するので、
**保存してコミットするたびに 102 MB が乗る**。無料枠 1 GiB に対して残りは約 440 MB＝**約4コミット分**。

### ⚠ 最初に立てた「地区ごとに additive scene へ分割する」案は、これ単独では答えにならない

ユーザー指摘（2026-08-15）: **「他の地区も今後岡部邸のような状況になります」**。
そのとおりで、分割案の前提が崩れる。

岡部邸は部材キット（`EdoGotenKit`）で棟を組んだ結果、**1屋敷で 11,859 個の
PrefabInstance = シーン全体の 48%** を占めている。同じ作り込みを他の屋敷にもやれば
**1屋敷あたり約30 MB ずつ地区シーンに積まれる**。地区あたり5〜8屋敷なので、
地区シーンは 150〜250 MB に育ち、**分割で稼いだ5倍は数屋敷ぶんで消える**。

→ **分割は「今の重さを割る」だけで、「重くなり続けること」を止めない。**
止めるには**重さの発生源**に手を入れる必要がある。

---

## 2. 何にバイトが使われているか（実測 2026-08-15）

シーンファイルを YAML ドキュメント境界で分解して集計した。

| 種別 | 個数 | 容量 | 割合 | 1個あたり |
|---|---:|---:|---:|---:|
| **PrefabInstance** | 24,699 | **64.9 MB** | **63.3%** | 2,756 B |
| Transform | 33,109 | 9.9 MB | 9.7% | 313 B |
| **MonoBehaviour** | **28** | **9.4 MB** | **9.2%** | **351 KB** |
| MeshRenderer | 7,157 | 8.9 MB | 8.7% | 1,304 B |
| GameObject | 8,416 | 3.7 MB | 3.6% | 455 B |
| MeshFilter | 7,157 | 1.9 MB | 1.8% | 275 B |
| その他 | — | 3.8 MB | 3.7% | — |

**わかったこと2つ:**

1. **PrefabInstance が 63%。** 1ブロック 2,756 B。ビルダーは全部
   `PrefabUtility.InstantiatePrefab` で部材を置いており（`EdoNishiTameikeBuilder.cs:86` ほか
   全ビルダー）、置いた部材1つにつき `m_Modifications`（位置・回転・名前の上書き）を持つ
   ブロックが1つ生える。**部材を置けば置くほど直線的に増える。**

2. **MonoBehaviour 28個で 9.4 MB。** 正体は **`WaterBody` 6個**（`WaterBody.cs:33` の
   `[HideInInspector] public float[] snap`＝地形の高さ配列）。最大の1個で 5.17 MB。
   メモリ `pond-snap-overlaps-in-scene` の「鍋島池の snap が三屋敷全域を含む」が
   そのまま容量に出ている。**池6個でシーンの9%。**

### 検証した「効かない案」

- **`Object.Instantiate` に変えてプレハブリンクを切る** → 効かない。
  単体プレハブなら 2,756 B が GameObject(455)+Transform(313)+MeshFilter(275)+MeshRenderer(1,304)
  = 2,347 B になるだけ（15%減）。**複数オブジェクトのプレハブでは逆に増える**
  （PrefabInstance は部分木まるごとを1ブロックで表すため）。

---

## 3. 方針 — 発生源に効く順に3つ

### B1. `WaterBody.snap` をシーンの外に出す（9.4 MB / 9%）

地形の高さ配列を YAML のテキストで持っているのが無駄。float 1個が10〜12バイトの文字列になる。

- 退避先はバイナリ資産（`.asset`）か `Library/`。**`EdoTerrainSnapshot` が既に
  `Library/` へ退避する仕組みを持っている**ので、それに寄せる
- 影響範囲が `WaterBody` 1クラスに閉じており、**作業手順は何も変わらない**
- 鍋島池の snap が三屋敷全域を含む件（`pond-snap-overlaps-in-scene`）も同時に見直す

### B2. 屋敷1つ ＝ プレハブ1つ（スケールの答え）

**ユーザー指摘への直接の回答がこれ。** 各屋敷を `.prefab` 資産として外に出し、
シーンには**配置の参照だけ**を残す。

実測（岡部邸を書き出して測った）: **`.prefab` = 31.2 MB / PrefabInstance 11,859**。
**総バイト数は減らない** — シーンからプレハブへ移るだけ。効くのは**粒度**のほう。

| | 今 | B2 のあと |
|---|---|---|
| 岡部邸を触る | 102 MB（シーン全体） | 31 MB（岡部のプレハブだけ） |
| 大和守を作り込む | 102 MB＋以後ずっと増える | その屋敷のプレハブだけ |
| 屋敷が増えたとき | シーンが単調に膨らむ | **他の屋敷のファイルは1バイトも変わらない** |

地区分割が「地区ごと」の粒度なのに対し、これは**屋敷ごと**。分割より細かく、
**屋敷が増えても劣化しない**。地区分割は B2 を入れれば不要になる（シーンが数MBまで落ちるため）。

⚠ **手順が1つ増える。** プレハブインスタンスの中を直接いじると、変更が
`m_Modifications` としてシーンに積まれて元の木阿弥になる。
**屋敷を触ったら最後に「プレハブへ適用」する**必要がある。
これは機械化できる（`Edo/屋敷/プレハブへ適用` メニュー＋ビルダー側の後始末に組み込む）。

### B3. 完成した棟を焼き固める（総バイト数を実際に減らす唯一の手）

棟1つが約800部材。**完成した棟のメッシュを1つに結合**すれば、
PrefabInstance 800個（約2.2 MB）が GameObject+MeshFilter+MeshRenderer 1組（約2 KB）＋
メッシュ資産（バイナリ）になる。岡部邸のプレハブは 31 MB → 数MB の見込み。

- **部材単位の編集ができなくなる**ので、**「完成した」と判断した棟にだけ**かける
- 元に戻せるよう、焼く前の部材構成は残す（別プレハブ or ビルダーで再生成できる状態）
- **B2 を入れてから、効果を測って必要なら**やる。先にやる必要はない

---

## 4. 進め方

**B1 → B2 →（測って必要なら）B3** の順。理由:

- B1 は影響が1クラスに閉じ、手順が変わらず、9% がすぐ取れる。**足慣らしとして最適**
- B2 は手順が変わる（プレハブへ適用）。1屋敷で試してから広げる
- B3 は不可逆性が高い。B2 の効果を見てから判断する

### B1 の手順

1. `WaterBody.snap` の退避先を決める（`.asset` か `Library/`）
2. 移行コードを書く。**既存の6個の snap を失わないこと**（`WaterBaker` の Recarve が
   snap に依存している。`waterbaker-ty-offset-bug` の復旧手順も確認）
3. 1つの池で試し、水面と地形が変わらないことをスクリーンショットで確認
4. 残り5個。シーンサイズを測って記録

### B2 の手順（1屋敷で試す）

**最初の1つは岡部邸**。理由: 単独で31 MB＝効果が最も大きく、失敗しても影響が1屋敷に閉じる。

1. `Edo_Yashiki_OkabeChikuzen` を `Assets/Edo/Prefabs/Yashiki/OkabeChikuzen.prefab` へ書き出し、
   シーン側をそのインスタンスに置き換える
2. シーンサイズを測る（102.5 MB → 約71 MB の見込み）
3. **ビルダーを試走させ、変更がシードに積まれず、プレハブ側へ適用されることを確認**
4. 「プレハブへ適用」をメニュー化し、`EdoOkabeYashikiBuilder` の末尾に組み込む
5. 問題なければ他の屋敷へ

### 検証（各段階でコミット前に必ず）

```bash
ls -l Assets/Edo/Scenes/Akasaka.unity          # シーンサイズ
python3 docs/maintenance/measure_lfs.py        # LFS 保存量
```

Unity 側:
- transform 合計が **192,556** のまま（オブジェクトを失っていないこと）
- 該当箇所のスクリーンショットが作業前と一致すること
- ビルダー試走で**二重のルートが生えない**こと

---

## 5. 見込み

| | 現在 | B1後 | B1+B2後 | B1+B2+B3後 |
|---|---:|---:|---:|---:|
| シーン | 102.5 MB | ~93 MB | ~5 MB | ~5 MB |
| 岡部邸を触ったとき | 102.5 MB | 93 MB | 31 MB | 数 MB |
| 屋敷を1つ作り込んだとき | +30 MB が恒久的に乗る | 同左 | **その屋敷のファイルだけ** | 同左（かつ小さい） |

**B2 まで入れれば「屋敷を作り込むほど重くなる」という性質そのものが消える。**
これがユーザー指摘への答え。

---

## 6. 地区分割について

当初案（`Base` / `Sanno` / `Sanbezaka` / `Tameike` / `Akasaka_Machi` / `Toranomon` の
additive scene）は **B2 を入れるなら不要**。シーンが数MBまで落ちるので分割する動機が無い。

調査で分かった前提だけ残しておく（将来やるとしたら要る）:

- **地区をまたぐオブジェクトは問題ない。** Unity のシーンに空間的な境界は無く、
  シーンは「どのファイルに保存されるか」を決めるだけ。またぐ地物は常時ロードの
  シーンに置けばよい
- **問題になるのは参照だけ。** シーンをまたぐオブジェクト参照は保存できない。
  2026-08-15 時点で **0 件**（シーン内の MonoBehaviour は `WaterBody`×6 /
  `GeoMarker`×6 / Player系のみで、いずれも閉じている）
- ⚠ **`Group()` の落とし穴**: ビルダー14本が持つ `Group(root, child)` は
  `GameObject.Find(root)` が空振りすると**アクティブなシーンに同名ルートを新規作成する**
  （`EdoNishiTameikeBuilder.cs:126` が正典、他13本がコピー）。
  分割するならルート名→シーンの対応表を持つルータを**同時に**入れること
- ビルダーが他地区の成果物を掴んでいる箇所: 旧 `EdoSanbezakaBuilder.cs:416` →`Edo_Sanno_Sha`(EDO-0299 で廃止)/
  `EdoOkabeYashikiBuilder.cs:638` →`Edo_Sanno_Sha` / 旧 `EdoAzukarichiBuilder.cs:123` →`Edo_Tamachi_Bank`(同)/
  旧 `EdoTameikeKitaBuilder.cs:707` →`Water_KurodaKamoba`(同)
- 地形 `ModernTerrain` は1つで全ビルダーが読み書きする。分割できない。
  なお `ModernTerrain.asset` は **15.5 MB / LFS** なので、**造成や splat を伴う作業は
  シーンと別に 15.5 MB が乗る**。これはどの案でも減らない

---

## 7. 実施結果（2026-08-16）

| | 開始時 | 旧版削除 | B1(池) | B2(屋敷) |
|---|---:|---:|---:|---:|
| シーン | 245.9 MB | 102.5 MB | 93.2 MB | **3.06 MB** |
| プレハブ83軒 | — | — | — | 93.1 MB |
| snap 外部ファイル | — | — | 2.6 MB | 2.6 MB |

**1回のコミットで書き換わる量**

| 何をしたとき | 前 | 後 |
|---|---:|---:|
| 岡部邸を直す | 102.5 MB | 31.2 MB(LFS外) |
| 干場・虎ノ門を直す | 102.5 MB | 5〜7 MB(LFS外) |
| 大半の屋敷を直す | 102.5 MB | 0.1〜3 MB(LFS外) |
| シーン自体を直す | 102.5 MB | **3.06 MB** |

### ★ プレハブは LFS の対象外だった（効果がさらに大きい）

`.gitattributes` の `*.prefab text merge=unityyamlmerge` により、プレハブは**通常の git
オブジェクト**として保存される（`Assets/Edo/Scenes/Akasaka.unity` だけが LFS 指定）。したがって

- **93 MB のプレハブは LFS 無料枠を1バイトも消費しない**
- 通常オブジェクトなので **zlib 圧縮＋バージョン間の差分圧縮が効く**。
  屋敷を1軒直してもリポジトリに増えるのは差分だけ
- 最大の岡部邸 31.2 MB も GitHub の通常オブジェクト上限 100 MB 内

→ **LFS に乗るのは実質シーン 3.06 MB と地形 15.5 MB だけ**になった。

### 検証

- transform 合計 **192,556** が分割前と完全一致（オブジェクトを1つも失っていない）
- プレハブ参照切れ **0 件**
- 全域のスクリーンショットで欠落なし

### 運用（★ 忘れると事故る）

Unity は**プレハブインスタンスの組み替え**（子の付け替え・プレハブ由来の子の削除）を
禁止しており、**例外を投げずコンソールにエラーを出して黙って無視する**。
2026-08-16 実測: `Setting the parent of a transform which resides in a Prefab instance
is not possible`。解かずにビルダーを走らせると**動いたように見えて何も起きていない**。

これを人の記憶に頼らないよう、2箇所で自動化した:

1. **`Group()` の冒頭で自動的に解く** — `EdoYashikiPrefab.EnsureEditable(r)` を
   全ビルダー18本の `Group()`(ほかに `EdoSannoShaRebuild` の3箇所)に挿入済み。
   ここが唯一の共通の通り道なので、ステージ関数を個別に叩いても必ず通る。
   ⭐ **書き戻しの台帳に載る点でもある**(2026-09-21・EDO-0282②) — 早期 return より前で控える
2. **シーン保存時に自動で書き戻す** — `EdoYashikiPrefabAutoSave` が `sceneSaving` で
   `EdoYashikiPrefab.WriteBack(scene, Touched, markDirty:false)` を呼ぶ。
   `Edo/屋敷/保存時の自動書き戻しを切り替える` で off にできる

⭐ **書くのは「この巡で `EnsureEditable` を通ったルート」だけ**(2026-09-21・EDO-0282②)。
⛔ それ以前は「プレハブ資産があるのにインスタンスでないルート」を**無条件に**書き戻していた。
保存1回で **74本・128MB** を書き直し、git 上で実際に変わったのは **1本**(2026-09-21 に実測)。
⛔ 併せて `MarkAllScenesDirty()` をやめた — `sceneSaving` の最中に全シーンを dirty へ戻すので、
保存した直後にまた未保存に戻り、`EdoKoba` の `isDirty` 門が理由不明に閉じていた。
⚠ 台帳は `SessionState`(エディタ起動中だけ生存)。**再起動すると消える** — その網が
`sceneOpened` の検査(解けたまま残ったルートを名指しで ⚠)。

手動の道具: `Edo/屋敷/切り出し状況を検査`(「解けたまま」列と台帳の印が出る)
`Edo/屋敷/プレハブへ書き戻す(触った分だけ)` / `…(選択中)` / `…(全部・強制)`
`Edo/屋敷/編集のためにプレハブを解く(選択中)`

⭐ **中身はシーンが持っている。** 書き戻さなくても `Akasaka.unity` の中には在る(シーンが太るだけ)。
消えるのは「書き戻さずに `Revert All` する」「保存せずシーンを閉じる/入れ替える」ときだけ。
⛔ 例外は**作業場** — `Koba.unity` は gitignore なので中身が残らない。だから `EdoKoba.Open` は
書き戻していない屋敷が載っていると**門を閉じて消さない**。

⚠ **`Overrides ▾ > Revert All` は押さないこと。** 書き戻す前の手直しが消える。
隣の `Apply All` は安全（書き戻すのと同じ）。Revert にキーボードショートカットは
割り当てられていない（2026-08-16 に Unity のショートカット一覧で確認済み）ので、
押し間違いで起きることはない。押しても `Cmd+Z` で戻せる（ただし再コンパイルで履歴は消える）。

### 残っている宿題

- **B3（完成した棟の焼き固め）は未着手。** 岡部邸 31.2 MB の内訳は
  PrefabInstance 11,859 個。棟のメッシュを結合すれば数 MB まで落ちる
- **シーンを LFS から外せる。** 3.06 MB なら通常オブジェクトの上限内で、
  差分圧縮も効く。`.gitattributes` の該当行を消すだけ（要判断）
- **snap 矩形が必要範囲より大きすぎる**（鍋島池で14.9倍）。挙動が変わるので別件

---

## 8. B3 の前に入れた「段別の入れ子プレハブ」（2026-09-21・EDO-0282③）

§7 で 1軒＝1ファイルにしたが、**1軒が育ちすぎた**。施主の問い(2026-09-20)
「書き戻しと保存に10分掛かるのを改善できないか」の残りの梃子がこれ。

### 実測 — 42MB の内訳（`Edo_Yashiki_MatsudairaDewa.prefab`）

YAML のドキュメント境界で分解し、transform の親子と `m_TransformParent` を辿って
ルート直下の群ごとに合計した（`PrefabInstance` は stripped Transform ごと当該の群へ寄せた）。

| 段（ルート直下の群） | 容量 | ノード | 中身 |
|---|---:|---:|---|
| **Buildings**（Stage4 御殿） | **21.3 MB** | 7,821 | プレハブ参照 7,795 / 生 26 |
| **Niwa**（Stage7 庭） | **15.2 MB** | 5,155 | プレハブ参照 **0** / 生 **5,155** |
| Fuzoku（Stage6 造作） | 3.6 MB | 1,281 | 参照 1,265 / 生 16 |
| Kakoi（Stage2 外周） | 0.94 MB | 288 | 参照 287 |
| Ishigaki（Stage3） | 0.90 MB | 348 | 参照 347 |
| Mon（Stage5 門） | 0.015 MB | 6 | 参照 5 |
| Fences | 0.001 MB | 1 | — |
| 合計 | **42.0 MB** | 14,901 | |

**門の位置を 1cm 直すだけでも 42.0 MB が書き直されていた。**

### ⛔ 庭が 15.2 MB なのは「解き方」の副作用だった

庭だけ **5,155 個すべてが生のオブジェクト**で、プレハブ参照が1つも無い。
原因は `EnsureEditable` が `PrefabUnpackMode.**Completely**` で解いていたこと —
葉のプレハブ（木・キットの部材）の接続まで全部ほどく。ほどけたまま書き戻された段は、
**同じ中身が生のオブジェクトとして直列化される**。木は LOD まで1個ずつ展開されるので特に太る。
直前に建て直された Buildings は参照のまま残っているので、比べれば差が見える。

→ `EnsureEditable` / `EnsureRootEditable` を **`OutermostRoot`** へ改めた
（`Assets/Edo/Scripts/Editor/EdoYashikiPrefab.cs`）。

### 仕掛け

ルート直下の群を1段＝1プレハブとして `Assets/Edo/Prefabs/Scene/Parts/<ルート>__<段>.prefab`
へ出し、ルートのプレハブは**参照だけ**を持つ。

- `EdoYashikiPrefab.Group(rootName, child)` … **全ビルダー共通の一本道**。
  ルートの外側と、**辿った先頭の段だけ**を解く。他の段は繋がったまま
- `EdoYashikiPrefab.SplitStages` … 書き戻しのときに段を書く。
  **繋がったままで override 0 の段は開かない・書かない**（据え置き）
- `EdoYashikiPrefab.EnsureEditable` … 従来どおりの**安全側の入口**。
  段へ割ってあれば段も全部その場で解く。委ねていないビルダー17本はこちらを通るので、
  `Clear()` が黙って失敗することはない（速さの利は出ない）
- 割る対象は**名簿で明示**（`SplitRoots`）＋ 一度割ったルートは Parts の実在で自動的に続く。
  ⛔ 大きさの閾値で自動に決めない — 類型の79区画がある日いきなり Parts へ散る

### 実測（2026-09-21 に Unity で計測。⭕ 受け入れ試験7件すべて通過）

作業場シーン・松江松平で計測。**かっこ内が計測前の見込み。**

| 何をしたとき | §7 のあと | ③ のあと |
|---|---:|---:|
| 門（Stage5）だけ直す | 40.04 MB | **0.033 MB**（門 0.014 ＋ 本体 0.019）（見込み 0.015 ＋ 本体） |
| 庭（Stage7）を流し直す | 40.04 MB | **2.61 MB**（14.54 MB から 5.6 分の1） |
| 全段を建て直す | 40.04 MB | 28.13 MB（＝全段ぶん。速さの利は出ない） |

割った結果（本体は参照だけになる）:

| 邸 | 本体 | 段 | 合計 | transform（割る前→後） |
|---|---:|---:|---:|---|
| 松江松平 | 40.04 → **0.019 MB** | 7本 | 28.13 MB | 18,137 → 18,137 |
| 岡部 | 35.31 → **0.039 MB** | 15本 | 35.31 MB | 15,580 → 15,580 |
| 土井 | 14.41 → **0.019 MB** | 7本 | 14.41 MB | 5,334 → 5,334 |

⭐ **`Completely` の害は実地で確かめられた。** 庭を Stage7 で流し直すと 14.54 → 2.61 MB。
資産全体では **生のオブジェクト 5,202 → 58 / プレハブ参照 9,699 → 10,535**。
置いた点数はビルダーの報告どおり（木104 / 刈込437株 / 下草270 / 景石18 ＝ 830 の実体）で、
transform が 18,137 → 15,336 に減ったのは**生の展開が参照へ畳まれた分**であって、失われた物ではない。

⚠ **庭の縮みは Stage7 を流し直すまで出ない。**割っただけでは 14.54 MB のまま。

⚠ **松江松平の庭は流し直した結果へ入れ替わっている**（直近の置き方の入れ替えを経た値）。
普請検査の数値は測り直しが要る。

### 受け入れ試験（⚠ Unity が空いてから。全部通るまで「入った」と言わない）

作業場シーン（`Edo/普請/作業場を開く`・松江松平）で回す。

| # | すること | 通る条件 |
|---|---|---|
| 1 | `Edo/屋敷/段別の入れ子プレハブへ割る(選択中)` | `Parts/` に7本・本体が数十KB。`Edo/屋敷/切り出し状況を検査` の段の表が §8 の内訳と一致 |
| 2 | Stage5（門）だけ流して保存 | ログが `段 書1/据置6`。mtime が動くのは `__Mon.prefab` と本体だけ |
| 3 | Stage7（庭）を流して保存 | `__Niwa.prefab` がプレハブ参照に戻って縮む（15.2 MB からどれだけ落ちたかを記録する） |
| 4 | 全段を流す | console に `resides in a Prefab instance` が **0 件**。⛔ 1件でも出たら `Group()` を通っていない箇所がある |
| 5 | 割る前後で transform 合計 | 完全一致（オブジェクトを1つも失っていない） |
| 6 | 岡部・土井でも 1〜5 | 同上 |
| 7 | 委ねていないビルダー（山王など）を1本流す | `EnsureEditable` 経由で従来どおり通る（段へ割っていないので挙動は変わらない） |

### 結果（2026-09-21・⭕ 7件すべて通過）

| # | 結果 |
|---|---|
| 1 | ⭕ 7段 + 本体 0.019 MB。段の表が出る |
| 2 | ⭕ `段 書1/据置6`。mtime が動いたのは `__Mon.prefab` と本体だけ。庭の md5 は不変 |
| 3 | ⭕ 庭 14.54 → **2.61 MB**。`段 書1/据置6` |
| 4 | ⭕ console の `resides in a Prefab instance` は **0 件**（この巡の全ログ・全期間とも） |
| 5 | ⭕ transform 完全一致（3邸とも） |
| 6 | ⭕ 岡部15段・土井7段。岡部は割り直すと `段 書0/据置15`（＝二度割っても何も書かない） |
| 7 | ⭕ `EnsureEditable` が7段すべて解く（安全側の入口が生きている）。その後の保存は `段 書7/据置0` |

⭐ **急所だった試験2は当たった。** 繋がったままの段では `GetObjectOverrides` が 0 を返し、
据え置きが効く。ここが外れていれば③の利はゼロだった。

⚠ **段の接続状態を「保存の後」に測ると全部『解』に見える。** 本体を繋ぎ直すと段が入れ子になり、
`IsAnyPrefabInstanceRoot` が false を返すため。判定が走るのは本体を繋ぎ直す**前**なので実害は無い。
測るなら `SplitStages` のログ（`段 書N/据置M`）で見ること。

⛔ **段がよその資産の実体なら割らない**（2026-09-21 に門を追加）。割ると段が Parts の複製へ
繋ぎ替わり、手組みの資産が参照されないまま取り残される（規則1）。3邸の段は全部「生」なので
この枝には入らなかったが、手で作った群をルート直下に置けば入る。

---

## 9. 建て直しの輪そのもの — 作業場で回す（2026-09-21・EDO-0282①）

②③はどちらも「**保存**が書く量」を削った。①が削るのは**待ち**のほう。

### 実測 — 1回のエディタ起動で何秒がドメインリロードに溶けたか

`Logs/Editor.log` を `python3 Tools/Unity/reload_cost.py` で数えた（2026-09-21 07:11〜11:13 の1起動）。
「復元」は `AwakeInstancesAfterBackupRestoration` ＝ **開いているシーンを退避して戻す**時間。

| 開いていたシーン | 回数 | 合計 | 中央値 | うち復元 |
|---|---:|---:|---:|---:|
| **Akasaka** | 15 | **531.1 s** | 34.3 s | 397.3 s |
| Koba（作業場） | 2 | 11.2 s | **6.9 s** | 2.6 s |
| （シーンを開く前） | 2 | 5.7 s | 4.4 s | 0.0 s |

⇒ **その1起動だけで 428 秒（7分）が「赤坂を開いたまま C# を直した」ことに溶けた。**
費用はコンパイル（4〜6秒）ではなく**シーンの復元**なので、直す方法は「開くシーンを軽くする」しかない。

### なぜ作業場へ移れていなかったか — 隣が居ないから

作業場は 2026-09-19 からあるのに、17 回中 15 回は赤坂で回していた。理由は道具の側にあった:
**作業場には対象邸しか載らず、許容0の欠陥のうち「区域侵犯」「隣との隙・めり込み」が測れない**
（`docs/oki-kata.md` §4）。測れない以上、棟梁も普請検査も赤坂へ戻るしかない。

### 入れたもの

1. **足あとの台帳** `UserData/Koba/ashiato.json` — 赤坂の全ルートの外接矩形（XZ・世界座標）。
   `Edo/普請/作業場: 足あとを採る(赤坂で)` が採る。⛔ **名前から区画を当てない** — 邸名・プレハブ名・
   区画 id は三者とも綴りが違い、類型79／旧83／手組みで規則も違う。実体の座標で当たれば表は要らない。
2. **隣を載せる** `EdoKoba.Open(邸, 隣=true, 間合い=20m, 上限=8)` — 台帳から 20m 以内のルートを近い順に。
   段へ割ってある邸は**囲いの段だけ**（`Kakoi/Ishigaki/Mon/Fence/Saku/Nagaya`）＝ 松江松平 42MB → 1.8MB。
   隣は名前に **`(隣)`** が付く。これ1つで「ビルダーの `GameObject.Find(ルート名)` に当たらない」
   「書き戻しの台帳の名前と一致しない ＝ 書き戻らない」の両方が立つ。Scene ビューでも掴めなくしてある。
3. **赤坂を開いたままコンパイルしたら ⚠**（`EdoKobaNudge`）— 人の記憶に頼らない。
4. **手仕舞いで突きつける** — `edo_session.py release --resources unity` が上の表を出す（規則19の結線）。

### 受け入れ試験（2026-09-21・Unity で実測）

| # | 試験 | 通る条件 | 結果 |
|---|---|---|---|
| 1 | 赤坂で `足あとを採る` | ルートが台帳に載る。地形・光・カメラは載らない | ⭕ **167 ルート・2.5 秒**（置いたまま 10 件） |
| 2 | `Tonari("Edo_Yashiki_OkabeChikuzen")` | **実際に境を接する邸**が返る | ⭕ 土井・樹下・山王門前1・松江松平 ＝ `okabe_sashizu.json` の辺ごとの相手と一致 |
| 3 | 作業場を開く（松江松平） | 隣が載る。transform 合計が赤坂の 1/10 以下 | ⭕ 隣 7 件・**34,814 / 341,330 ＝ 10.2%** |
| 4 | その状態で C# を1行直す | ドメインリロードが 10 秒未満 | ⭕ **7.8 秒**（うち復元 2.0 秒）。赤坂は 34.3 秒 |
| 5 | 対象邸だけ触って保存 | 隣のプレハブの mtime が動かない | ⭕ 動いたのは `MatsudairaDewa` 本体と `__Mon` だけ。岡部・土井・鳥居・干場は不変、git の差分も 0 |
| 6 | 二度続けて開く | 屋敷も隣も二重にならない | ⭕ 2回とも root 10・transform 34,814（冪等） |
| 7 | 赤坂でコンパイル | ⚠ が1回出る | ⭕ `compilationStarted` に 1 本つながっており、赤坂で呼ぶと ⚠ が出た |
| 8 | `release --resources unity` | シーン別の秒数が出る | ⭕ 手仕舞いの出力に表が付く（この日の総計 635 秒・うち作業場なら要らなかった分 493 秒） |

⚠ **試験7 はコンパイルを起こさずに確かめた** — 赤坂でわざと再コンパイルすると 35 秒かかるので、
`compilationStarted` の登録本数を数え、ハンドラを直に呼んで ⚠ の有無を見た。作業場側（何も出ない）は
分岐が同じ1行なので測っていない。

⚠ **岡部の「囲いだけ」は 7,527 transform ある**（`Kakoi` 13.55MB に外周長屋が丸ごと入っているため）。
隣として載せても丸ごとの半分にしかならない。段の切り方を細かくすればさらに落ちるが、別件。

⭕ **二重に載る区画は消えた**（2026-09-21 12:08）。EDO-0299 ④ が旧邸 28 本を畳み、台帳は
167 → **139 件**（旧邸 8・類型 79・そのほか）で採り直した。⭐ **邸を消したら赤坂で
`足あとを採る` を押し直すこと** — 消えた邸が台帳に残ると隣の枠（上限8）を食う。
