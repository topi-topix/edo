# 検証の輪 — 「誰の目にも入っていない値」は未検査であって合格ではない

⭐ **この文書が答える問い**: 「指図をどれだけ精緻にしても、実装しないとどうにもならないのか」
(2026-09-02 ユーザー質問)。

⛔ **答えは「そのままの YES ではない」。**実測で裏を取った結果、より強くて使える形が出た。
本文書がその形と、機械で見張る手立てを定める。

---

## 1. 法則

> **書いた数値は、それを見せる図か検査へ届いて初めて検められる。**
> **届いていない値は「合格」ではなく「未検査」である。**
>
> ⭐ そして **輪には種類があり、種類ごとに見える欠陥が違う。**
> ⭐ **輪には値段があり、高い輪を先に回すと作った物を捨てることになる。**

前半を **閉じ(closure)**、後半を **順序(ordering)** と呼ぶ。⛔ 両方要る。
片方だけでは、それぞれ逆向きの事故が起きる(§4・§5)。

---

## 2. 輪は3種類ある。何が見えるかが違う

| 輪 | 回し方 | ⭕ この輪でしか出ない欠陥 | この輪では出ない欠陥 |
|---|---|---|---|
| **計算の輪** | 断面・縦断・方位・収支を**数式で**引く | 水が流れない(落差ゼロ・逆勾配)/ 帯どうしが重なる / 動線が繋がっていない / 建蔽率 | 図にしないと分からない「見え方」 |
| **図の輪** | 平面・断面・見付を**原寸で描いて見る**  | 並木に見える / 図と本文が食い違う / 撤回した説が図に残る / 抜きが描かれていない | 部材の実寸に依る取り合い |
| **実装の輪** | Unity に**建てて、レンダを見る** | 部材の実メッシュの見え方 / 屋根の掛かり / 駒どうしの隙間 / 材質 | 「水が流れない」— 建てても目に見えない |

⭐ **3つ目が在ることは、この法則の実質そのもの。** 計算と図をどれだけ精緻にしても、
**実装の輪でしか出ない欠陥は出てこない**。CLAUDE.md 規則5(部材どうしを中心で合わせない・
置いた駒の実メッシュから面を測って寄せる)と EDO-0096(数値QAの合格は run 単位の中央値
止まり — 駒ごとのばらつきはレンダで初めて見つかる)は、いずれもこの列の宣言である。

⛔ **逆も同じだけ強い。**「水が 60m を落差ゼロで流れない」「暗渠が上流へ昇っている」は
**建てても目に見えない**。実装の輪は万能ではなく、**計算の輪の代わりにはならない**。

---

## 3. 閉じ — 輪に入っていない産物は、無いのと同じ

⛔ **目視では見つからない。**松江松平は同じ型を**3度**見逃し、4度目で機械化した。

産物が輪から外れる形は3つある。**すべて実例が在る:**

| 型 | 何が起きるか | 実測された実例 |
|---|---|---|
| **① 孤立** | 書いたのに**呼んでいない** | 松江松平 `garden_east_svg` / `garden_section_svg` / `neishi_table` / `neishi_check`(実際にコミットされた状態で4件同時)/ ⭐ **岡部 `walls_table`(段の土留めの一覧表)は現在も一度も描かれていない** |
| **② 捨て** | 呼ぶが**戻り値を誰も受け取らない** | 松江松平が自邸の機械照合に「残る穴」として自己申告(2026-09-02) |
| **③ 出していない** | 測ったのに**成果物に載せない**(stdout 止まり) | ⭐ **岡部「法面24件が要約に出ず切盛図のキャプションにしか無い」「抜きが6枚中5枚の図に描かれていない」**(検図 2026-09-02)/ **外堀「README の QA 不合格が指図に無い」**(検図 2026-09-01) |

⭕ ①②は **`Tools/Sashizu/wiring_gate.py`** が機械で鳴らす(全邸共通・移植不要)。
③は静的には見えないので、同ツールの `--surfaced` で**実行ログ × 成果物 HTML**を突き合わせる。

```bash
python3 Tools/Sashizu/wiring_gate.py                       # 全邸の生成器を見る
python3 Tools/Sashizu/build_<邸>_sashizu.py 2>&1 | tee /tmp/run.log
python3 Tools/Sashizu/wiring_gate.py --surfaced /tmp/run.log docs/Sashizu/<邸>_sashizu.html
```

⛔ **`build_matsudaira_dewa_sashizu.py` の `check_wiring_check` を各邸へ写さないこと。**
あれは当邸固有の報告経路(`plane_check` の束・`main()` の `WARN`)に結ばれていて、
移植には読み替えが要る — **移植の写しは 2026-08-25 に土井が偽の不一致28件を出した型**。
⭕ `wiring_gate.py` は外からソースを読むだけで、邸ごとの構造を仮定しない。

### ⛔ 0件は「合格」ではない

`wiring_gate.py` が見るのは**結線**だけ。次は原理的に見えない:

- **恒真の検査** — 走って報告されるが、何を壊しても0件を返す。
  ⭐ 実例: 岡部 `kekkai_check` は「**結界を全部消す**」でも0件(検図 2026-09-02)/
  岡部 `program_check` の文字列一致の恒真(EDO-0102)。
  ⛔ **破壊試験でしか出ない**(EDO-0106「破壊試験は必ず3通り」)。
- **検査そのものが無い領域** — → EDO-0106(検査の**型**の欠落)の持ち場。
- **建てないと見えない不良** — → §2 の3行目。
- ⭐ **欄そのものが無い値** — ↓ 次項。

### ⛔ 欄が無い物は、どの関門にも掛からない(2026-09-06 岡部・土井)

⭐ **`wiring_gate` も `decision_gate` も「書いた物の結線」を見る道具**である。
⇒ **指図に欄が無い属性は、両方とも素通りする。**0件は「その属性が正しい」ではなく
「**そんな属性は存在しないことになっている**」という意味でしかない。

**実例 — 御殿の屋根(EDO-0149)。全邸で、屋根が考証の輪の外にあった。**
穴の入口は邸ごとに違い、**どちらも関門は緑だった**:

| 邸 | 入口 |
|---|---|
| 土井 | 欄は**書いたが検査していない**(`roof` はあるが `band_check` が無かった) |
| 岡部 | **欄を書いてすらいない**(`roofs.Goten_*` に `kawara` が1つも無い) |

⛔ そのうえ各邸の指図に「屋根は図示のための概略・**突き合わせの対象外**」という断りが
**22〜24箇所**あり、これが穴を正当化していた。⇒ 結果、**梁間10間超(18m超)を一枚の
小屋組で飛ばす棟**が土井5・松江松平7。⚠ 現存例で最大の [山脇武家屋敷門]A ですら梁間 4.7m。

⭕ **対策の型**: 「**部材や実装が持つ属性を、指図の欄として先に立てて U で埋める**」。
U で埋まった欄は `certRulings` に載り、考証方の目に入り、検査を書く相手ができる。
⛔ 欄を立てないまま実装へ渡すと、**棟梁が発明する**(規則17 の事故の型そのもの)。

⭕ **「輪の外」を探すときは、「検査が無い」と「欄が無い」を同じ重さで見る。**
⚠ 後者のほうが見つけにくい — 前者は `grep` で拾えるが、後者は
**「実装が使っている属性を数え上げて、指図の欄と突き合わせる」**という別の作業が要る。

### ⛔ 欄はあるが刷られていない — 「誰が決めたか」の輪(2026-09-06 土井)

⭐ **正典にあっても、図に刷られなければ読む人には無いのと同じ。**
土井の `certRulings` は `program_check` が読むだけで**どの表にもなっていなかった**。
⇒ 「⛔ ユーザー裁定ではない」と正典に書いても**誰の目にも触れない**。
⭕ **表に刷るようにした途端、誤帰属が一度に見えた** — 最終的に **9件**(池1 / 御殿の屋根6 /
井戸1 / 表門1)。⛔ **関門は 3/3 緑のままだった。**

⭐ **型**: **「誰が決めたか」の欄に、渡された役・検めた役が書かれる。**
⛔ 意匠の役(庭方)・検分の役(考証方/検図方)・実装の役(棟梁)は、**渡された事項の出自にならない**。
⚠ 悪意なく起きる — **「その巡で最後に触った役」を書いてしまう**から。

⛔ **いちばん危ないのは、ユーザー裁定が役の名で記録される**こと。土井では2件あり、
どちらも **典拠が取れない値**(四周入側 / 表門の番所・潜戸)= ⭐ **出自がユーザーであることが
唯一の正当性**という、最も消えてはいけない行だった。⛔ 役の名になっていると、
**次の巡で普請奉行の一存で覆せる行に見える。**

⭕ **形**:
1. **列を2つに割る** — **決めた者** と **確度を裁定した者**。⭐ 巻き戻すとき問うべきは
   「**誰の決定を覆すのか**」なので両方要る
2. **ユーザー裁定に印**を打ち「⛔ **覆すのに普請奉行の一存では足りない**」と刷る。
  ⚠ 件数は「**この表の中で N 件**」と書く( 邸の総数と読まれる)
3. ⛔ **分からない所は分からないと書く**(「 どの巡かは記録に無い」)。**推測で埋めない**
4. ⭕ **裁定表を必ず図に刷る**

⚠ **関門(`review_gate`)は「誰に検められたか」を見る道具で、「誰が決めたか」の正しさは見ていない。**

### ⛔ 検査は走っていて0件だが、読んでいる入力が古い(2026-09-06 土井)

隣家の塀の埋没を測る検査は隣家の指図を読むが、⛔ **隣家が動いたことを検知する仕組みが無かった**。
⇒ 松平が土井境の run を落としたのに、当家の図は**その run を載せたまま**だった。
⭕ **隣家の指図のハッシュを持ち、変わっていたら「回し直せ」を非0で返す**条を立てた。
⚠ **書き戻しを自動化しない**(自動化すると二度と鳴らない)。
⛔ **比べる先は `git show main:`** — ディスクの実物だと**他邸が編集中に保存しただけで赤くなる**、
worktree の `HEAD` だと**自分が取り込んだ姿と常に一致して検査が死ぬ**。
⚠ **数字を読む側も同じ基準へ揃える** — 片方がディスク・片方が git だと
「変わっていない」と言いながら図が動く。

⇒ ⭐ **検査の結線だけでなく、入力の鮮度も輪に入れる。**

---

## 4. 順序 — 高い輪を先に回すと、作った物を捨てる

⛔ **「骨格が固まったら、細部を詰める前にとりあえず建てて見る」は、そのままでは誤り。**
実装の輪は**いちばん高く、やり直しもいちばん高い**。実測された代償:

- CLAUDE.md 規則2 — 指図を飛ばして**2度全面差し戻された**。「図なら数分、建ててからだと数時間」
- CLAUDE.md 規則18 — 松江松平の庭が**庭方に一度も検められないまま Stage7 まで実装され**、
  差し戻された(2026-09-01)。⭐ 庭方は致命5件で不合格 → **庭方自身が全面を再設計**。
  ⛔ 先に建てた分は効かなかった。
- メモリ — 岡部邸の実装記録(13棟・建蔽率33.7%)は 2026-08-23 に**指図ごと失効**。

⭐ **だから順序は「安い輪から」。** 正しい形:

> ⭕ **その欠陥を見られる輪のうち、いちばん安いものを先に回す。**
> ⛔ **安い輪で見えるはずの欠陥を、高い輪に初めて見つけさせない。**
> ⭕ **実装の輪へ進むのは、それでしか見えない型を取りに行くときだけ。**
> ⛔ **そのときも、安い輪(検分役)が緑になってから。**

⚠ **これは移行期間の裁定(CLAUDE.md 規則18・案B)と矛盾しない。**移行中は
「検分を通すまで作業を続けてよい」。⛔ ただし**ユーザーへ見せる前には必ず通す**の一線は動かない。

### ⚠ 「実装しないと分からなかった」を、鵜呑みにしない

実装後に出た指摘を、**そのまま「実装の輪でしか出ない」と数えない。**⭕ 1件ずつ問う:

> **この欠陥を見られる、もっと安い輪はあったか。**

- 「駒どうしに 0.72m の隙間」→ ⭕ 実装の輪でしか出ない(部材の実メッシュに依る・規則5)
- 「屋根が無い」→ ⛔ **有無は数えられる** = 計算の輪で出る(完備の検査の欠落であって、
  実装の輪の手柄ではない)

⛔ ここを混ぜると「建てなければ分からなかった」が免罪符になり、**足りない検査が足りないまま
残る**。⭐ 実装後の指摘は、**次にどの輪へ検査を1本足すか**の材料として読む。

---

## 5. 使い方 — 各巡で問う4つ

1. ⛔ **新しい設計値を入れたら、同じ巡でそれを描く図か表を出したか。**
   図の無い所は「未検査」であって「合格」ではない。
2. ⛔ **検査を書いたら、同じ巡で報告経路へ繋いだか。** → `wiring_gate.py` が見張る。
3. ⛔ **その検査は、壊したら鳴るか。** → 破壊試験3通り(EDO-0106)。 素で0件は当たり前。
4. ⛔ **実装後に出た指摘は、本当に実装の輪でしか出ない型か。** → §4 の問い。

---

関連: **[EDO-0106]** 検査の**型**の欠落(本文書は「型は在るが輪に入っていない」を扱う。持ち場が違う) /
**[EDO-0096]** 集約統計は個体のばらつきを隠す / **[EDO-0102]** 存在しか見ない判定 /
CLAUDE.md 規則2・5・17・18 / [reporting-protocol.md](reporting-protocol.md)

---

## 第4型 — 走っているが「件数を刷る経路」に届かない

⭐ **2026-09-02 に岡部の普請奉行が申し送った型。** **孤立していないが要約に出ない検査は、
孤立と実害が同じ。**

岡部の `batter_check`(法面)は**呼ばれている**のに、結果が**切盛図のキャプションの中にしか**
出ない。そのため要約だけを見た指図方も普請奉行も「全項目0件」と報告し、**24件を1巡見落とした**。

⛔ **`--surfaced`(実行ログ × 成果物)では拾えない** — 実行ログにそもそも1行も出ないため。

| | 形 |
|---|---|
| ⭕ 良い | `bad = f(d)` → `print("… %d 件" % len(bad))`(**0件でも必ず刷る**) |
| ⭕ 良い | `bad += f(d)` → `return bad`(呼び出し側が刷る責任を持つ) |
| ⛔ 悪い | `bb = f(d, ter)` → `h.append('<p class="cap">法面の検査: %s' % …)` **だけ** |

⭐ **判定の一線は「数のまま運ぶか、文字列へ埋めるか」。**`out.append((題, len(msg)))` は
呼び出し側が刷れるので良い形、`h.append('…%s' % len(bb))` はそこで図の中に消えるので悪い形。
`wiring_gate.py` は **種ごとに閉包を取り**、`print` か `return` へ届かない検査だけを鳴らす。

⚠ **この型を実装するときに二度踏んだ落とし穴**(2026-09-02):
1. **全部の種を1つの taint に混ぜた** — 別名へ伝わった時点で「どの種が届いたのか」が
   分からなくなり、当邸の感度試験(`out.append((題, len(ex_msg)))`)を**誤報**した。
   ⭕ 種ごとに閉包を取ること。
2. ⭐ **道具自身が第4型だった** — `mute` を計算しているのに `main()` で刷っていなかった。
   ⛔ **検査を書く人ほどこの型を踏む**(松江松平3度・岡部3件)。書いた直後に自分へ当てること。

### ⭐ 関門そのものにも輪が要る — 自己検査(2026-09-02 岡部の提案)

⛔ **この道具は一度、免除を広げすぎて全29検査が免除に落ち、検出が丸ごと死んだ。**
⚠ そのとき道具は **「0件」を出しており、外からは「直った」と区別が付かなかった**
(気づけたのは岡部が実物で回したから)。⇒ **検出が死んでいないこと自体を機械で見張る。**

    python3 Tools/Sashizu/wiring_gate.py --selftest
    python3 Tools/Sashizu/decision_gate.py --selftest
    python3 Tools/Session/config_doctor.py --selftest   # 道具改め(設定そのものの関門・docs/teire.md)

⭕ 健全な版で鳴らないこと + 4型それぞれの欠陥を仕込んだ版で必ず鳴ることを確かめる。
⛔ 落ちたら、邸の生成器が0件でも**それは合格の意味を持たない**。

⭐ **関門を作ったら、必ずその関門の自己検査も同じ巡で作ること。**
「0件」は「検査が通った」とも「検査が死んでいる」とも読める。⛔ **両者を区別する術が無い関門は、
無い関門より悪い**(在ると思って誰も見なくなる)。

### ⭐ 関門の結果が相手の報告と食い違ったら、コードでなく**道具の版**を先に確かめる

⚠ 2026-09-02、岡部は `sync-tools` を回す前の古い版で回して**誤検出7件**を見ており、
松江松平の「直した」という報告と食い違った。⛔ **「隣が直したと言っているのに手元で鳴る」ときは、
まず自分の道具の版を疑う。**関門は全 worktree へ配付される作りなので、
配付前と配付後で結果が変わる。

### ⛔ 破壊試験は「書いた」だけでは記録にならない — 壊し方がその検査へ届くかまで確かめる

⚠ **2026-09-02 岡部の実例。**破壊試験のログに「**中門の開口を潰す → 反応する**」と書いてあったが、
⛔ **どの動線も中門を通っていない**ので、その壊し方は原理的にその検査へ届かない。
⇒ ログは「試した」と読めるのに、**検査は一度も試されていなかった**。
⛔ **これは静的解析の外**(`--selftest` でも `wiring_gate.py` でも見えない)。
⚠ この一件が、岡部で **27件を1巡隠した**。

⭕ **破壊試験は、壊し方が本当にその検査へ届くかまで確かめて初めて記録になる。**
⛔ 「破壊試験を書いた」という記録が、その破壊試験が意味を持つことを保証しない。
⭕ 確かめ方はひとつだけ — **壊した版で件数が実際に増えるのを自分の目で見る**
(「増えるはず」と書かない)。⭐ 記録には**壊す前と後の件数**を並べて書くこと。
